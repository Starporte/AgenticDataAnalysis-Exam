[pytest]
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --cov=backend --cov-report=term --cov-report=html
markers =
    integration: mark test as integration test
    security: mark test as security test
    persistence: mark test as persistence test
